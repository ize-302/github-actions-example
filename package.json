{
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "supertest": "^6.3.3"
  },
  "scripts": {
    "start": "node server.js",
    "test": "jest --watchAll --maxWorkers=1",
    "test:ci": "jest --watchAll --maxWorkers=1"
  },
  "devDependencies": {
    "jest": "^29.5.0"
  }
}
